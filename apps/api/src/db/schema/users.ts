// Tabela `users` removida — auth 100% via Better-Auth (tabela `user` em schema/auth.ts).
// Removido do schema em v0.2 pra eliminar conflito entre FK uuid e BA.id text.

export const users = undefined;
