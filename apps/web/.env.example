# ──────────────────────────────────────────────────────────────
#  BunkerBarbershop WEB — variáveis de ambiente (exemplo)
#  Copie para .env e ajuste. NUNCA commite .env.
# ──────────────────────────────────────────────────────────────

# URL da API. Vite faz proxy /api → :3001 automaticamente (ver vite.config.ts)
# Em prod, aponta pro dominio do backend (ex: https://api.bunker.app)
VITE_API_URL=http://localhost:3001
