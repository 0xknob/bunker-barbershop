// Placeholders — implementados nos commits 13 (CUSTOMER), 14 (BARBER), 15 (OWNER)
import { Window } from '../components/ui/Window';

export function ClientPanel() {
  return <Window title="Painel do Cliente"><p>Em construção (commit 13).</p></Window>;
}
